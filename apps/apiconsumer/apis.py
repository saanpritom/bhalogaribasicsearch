from apps.searchapp.configs import *


#Link of all api endpoints
api_endpoints = {
    'all_cars': str(base_url) + '/api/v1/cars/',
    'single_car': str(base_url) + '/api/v1/car/',
    'car_search': str(base_url) + '/api/v1/cars/search/',
}
